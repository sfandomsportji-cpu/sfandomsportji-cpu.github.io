name: Temporary audio diagnostic

on:
  push:
    branches: [audio-refresh-20260825]

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Confirm runner
        run: |
          echo "runner-ok"
          python --version
          git status --short
