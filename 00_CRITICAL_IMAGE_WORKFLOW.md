# 🚨 CRITICAL — SFANDOM Image Extraction Workflow

> 중요도: **최상 / 핵심 운영 업무**
> 적용 대상: **Instagram / Blog / SFANDOM 콘텐츠 제작용 이미지 수집**

## 기본 실행 규칙

사용자가 `관련사진 N장`, `사진 추출`, `이미지 찾아줘`와 같은 요청을 하면 아래 기준을 기본값으로 적용한다.

1. **실제 뉴스·공식 사진만 사용한다.**
2. **AI 생성 이미지는 사용하지 않는다.**
3. 사진 자체에 아래 표시가 박혀 있으면 **제외한다.**
   - 언론사 로고·워터마크
   - 방송사 로고·워터마크
   - 개인 블로그 로고·서명
   - SNS 계정명·닉네임
   - 채널 로고·편집앱 워터마크
4. 구단 유니폼 로고, 경기장 광고판 등 **현장에 원래 존재하는 요소는 제외 사유가 아니다.**
5. 가능한 한 **원본 수준의 큰 해상도**를 우선한다.
6. 같은 선수·같은 장면의 반복을 줄이고 **다양한 선수·장면**을 구성한다.
7. 기본 출력은 **채팅창에 바로 표시**한다.
8. 사진 표시 후 마지막에는 **현재 표시한 사진들만 대상으로 하는 일괄 다운로드 버튼 1개**를 제공한다.
9. 검색 결과만 보여주는 것을 완료로 간주하지 않는다. 사용자가 바로 확인할 수 있도록 실제 이미지가 표시되어야 한다.

## 운영 목적

이 업무는 단순 이미지 검색이 아니라 SFANDOM의 Instagram·Blog 핵심 제작 공정이다.
목적은 사람이 기사마다 들어가 사진을 캡처하는 반복 작업을 줄이고,
여러 출처에서 다양한 이미지를 빠르게 수집·선별하는 자동화에 있다.

## 실패 기준

아래 중 하나라도 해당하면 작업을 완료로 보고하지 않는다.

- 생성 이미지가 섞임
- 사진 자체에 언론사/방송사/블로그/SNS 워터마크가 있음
- 저해상도 썸네일 위주
- 중복·유사 장면이 과도함
- 채팅창에서 실제 사진을 확인할 수 없음

이 문서는 SFANDOM 이미지 수집 업무의 **최우선 기준 문서**로 취급한다.


## 다운로드 버튼 운영 규칙 — 중요

- **폴더로 먼저 추출하지 않는다.**
- **ZIP 파일을 미리 만들지 않는다.**
- **이전 폴더나 이전 다운로드 묶음을 재사용하지 않는다.**
- 매 작업마다 **그 턴에 실제로 띄운 현재 사진 목록**을 기준으로 다운로드 버튼을 새로 만든다.
- 다운로드 버튼은 현재 사진 URL을 한 번에 내려받는 용도로만 사용한다.
- 사용자가 명시적으로 폴더/ZIP 저장을 요청한 경우에만 별도 파일 묶음을 만든다.
